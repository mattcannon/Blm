build_settings:
    clone_depth: 5
    verbose: true
    ignore:
        - "vendor"

setup:
    composer:
        action: "install"

test:
    php_unit:
#    php_docblock_checker:
#        allowed_warnings: 10
#        path: "src"
